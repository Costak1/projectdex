services:
  cyber-watch-suite:
    build: .
    container_name: cyber-watch-suite
    ports:
      - "8001:8001"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=${TZ:-America/New_York}
