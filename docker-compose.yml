services:
  cyber-watch-suite:
    build: .
    container_name: cyber-watch-suite
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/New_York
